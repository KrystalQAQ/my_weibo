# 环境变量示例文件
# 复制此文件为 .env.local 并根据需要修改

# ==================== 代理配置 ====================

# Cloudflare Worker URL（可选）
# 如果设置了此变量，将使用 Cloudflare Worker 作为代理
# 如果留空，将使用 Nginx 反向代理（相对路径）
# VITE_WORKER_URL=https://your-worker.workers.dev
VITE_WORKER_URL=https://wb.254253.xyz

# ==================== 使用说明 ====================

# 方案 1: 使用 Nginx 反向代理（推荐）
# VITE_WORKER_URL=
#
# 优势：
# - 无请求限制
# - 统一部署
# - 更低延迟（国内用户）
#
# 要求：
# - 使用 nginx-full.conf 配置
# - Docker 容器包含代理配置

# 方案 2: 使用 Cloudflare Worker
# VITE_WORKER_URL=https://your-worker.workers.dev
#
# 优势：
# - 全球 CDN 加速
# - 自动扩展
# - 高可用性
#
# 限制：
# - 免费版 10 万请求/天
# - 需要单独部署

# 方案 3: 混合方案
# 开发环境使用 Worker，生产环境使用 Nginx
# .env.development: VITE_WORKER_URL=https://your-worker.workers.dev
# .env.production: VITE_WORKER_URL=
